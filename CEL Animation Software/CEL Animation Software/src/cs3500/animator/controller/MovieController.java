package cs3500.animator.controller;

/**
 * An interface which represents a Controller for a Movie. It connects a model to a view.
 */
public interface MovieController {

  /**
   * Runs the MovieController.
   */
  void run();
}
